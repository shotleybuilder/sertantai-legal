/**
 * Database Schema Types
 *
 * These are placeholder types for template/example files.
 * The main data type used in this project is UkLrtRecord from electric/uk-lrt-schema.ts
 */

/**
 * Case type - placeholder for template files
 * Not used in production code
 */
export interface Case {
	id: string;
	title: string;
	description?: string;
	status?: string;
	inserted_at: string;
	updated_at: string;
}
