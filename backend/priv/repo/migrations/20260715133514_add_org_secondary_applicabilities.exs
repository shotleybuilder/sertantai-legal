defmodule SertantaiLegal.Repo.Migrations.AddOrgSecondaryApplicabilities do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:org_secondary_applicabilities, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :organization_id, :uuid, null: false
      add :source_id, :text, null: false
      add :status, :text, null: false, default: "unreviewed"
      add :source, :text, null: false
      add :notes, :text
      add :reviewed_at, :utc_datetime_usec
      add :reviewed_by, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:org_secondary_applicabilities, [:organization_id, :source_id],
             name: "org_secondary_applicabilities_unique_org_source_index"
           )
  end

  def down do
    drop_if_exists unique_index(:org_secondary_applicabilities, [:organization_id, :source_id],
                     name: "org_secondary_applicabilities_unique_org_source_index"
                   )

    drop table(:org_secondary_applicabilities)
  end
end
